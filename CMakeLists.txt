#Specify version
cmake_minimum_required(VERSION 3.20)
#Project name
project(Cavern)

add_subdirectory(Codebase)

#Add all files that want to be included in the .exe here

#Add the executables to be run as test here?
